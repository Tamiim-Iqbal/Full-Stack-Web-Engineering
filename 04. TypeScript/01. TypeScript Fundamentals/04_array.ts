const numbers : number[] = [1, 2, 3, 4, 5];
console.log(numbers);       // [1, 2, 3, 4, 5]

// numbers.push('6'); // Error: Argument of type 'string' is not assignable to parameter of type 'number'.

const myLocation: [number, number] = [23.456, 45]   
console.log(myLocation);     // [ 23.456, 45 ]

const entries: [string, number] = ['age', 24]
console.log(entries);       // [ 'age', 24 ]
